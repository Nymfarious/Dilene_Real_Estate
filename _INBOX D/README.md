# _INBOX D

Manual drop folder. Put files here for Claude to pick up in a session — screenshots,
PDFs, exports, capture photos, anything awkward to paste into chat.

Contents are git-ignored; this README is the only tracked file, so the folder survives
a fresh clone. Nothing here is canon — anything worth keeping gets promoted into
`docs/`, `data/`, or `capture/` with a commit.
