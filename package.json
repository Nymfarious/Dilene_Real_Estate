{
  "name": "dilene-real-estate",
  "private": true,
  "version": "0.1.0",
  "description": "For Dilene — a cinematic 3D property experience, built as a gift.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "plan": "node tools/plan-render/render.mjs",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lenis": "^1.3.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
