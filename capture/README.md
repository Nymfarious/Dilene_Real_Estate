# capture/

One folder per property: `capture/<property-id>/` with `sketch.jpg`, `measurements.md`, `notes.md`, `photos/<space-id>/`.
Originals stay in cloud storage. Only downsized photos (≤ 1600 px, ≤ 400 KB) that a space actually references are committed.
See `docs/capture-protocol.md`.
